# Godot editor cache and imported resources
.godot/

# Export output and signing credentials
/build/
# export_presets.cfg is tracked: a debug preset carries no secrets, because debug
# signing uses the editor keystore, not the preset. Never put release keystore
# credentials in it.
*.apk
*.aab
*.exe
*.pck

# Windows
Thumbs.db
